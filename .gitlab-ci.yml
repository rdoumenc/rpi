image: rdoumenc/build-rpi ENTRYPOINT=/bin/bash
build:
  script:
  - ls
  - pwd
  - env
  - uname -a
  - ls /
  - mkdir build
  - source /workspace/yocto/poky/oe-init-build-env /workspace/build && bitbake rpi-custom-image